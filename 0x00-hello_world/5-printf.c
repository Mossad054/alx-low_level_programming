#include<stdio.h>
/**
 * main - a program that writes proper grammer function
 * Return: 0 (succeed)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (o);
}
